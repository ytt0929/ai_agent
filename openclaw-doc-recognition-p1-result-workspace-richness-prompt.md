# OpenClaw 提示词：资料识别 P1 结果区业务表达增强

请读取资料识别模块相关代码，并执行 P1 增强。本轮目标是在 P0 默认演示态基础上，把右侧「字段识别 / 交叉比对」结果区做得更像授信尽调资料识别成果，而不是普通 OCR 字段列表。

本轮不要做全页面大重构，不要改其他模块，不要改 `token.css`。重点增强右侧结果区的信息架构、业务表达和 Element Plus + token.css 风格一致性。

## 重点文件

请优先修改：

- `src/pages/DocRecognitionPage.vue`
- `src/stores/docRecognition.js`

如果需要新增本模块内部 computed/helper，可以写在这两个文件内。不要抽到全局，不要改其他模块。

## 当前状态

P0 已完成：

```text
默认企业：唐山物桥商贸有限公司
默认文件：银行流水摘要.pdf
右侧字段识别不为空
交叉比对 tab 有唐山物桥数据
```

但现在右侧结果仍然偏“字段列表”，demo 讲起来像 OCR 工具，不像授信尽调资料识别工具。

P1 要实现：

```text
字段识别：从平铺字段 → 分组字段 + 摘要卡
交叉比对：从普通列表 → 业务结论面板
同步价值：告诉用户这些结果会进入尽调/报告哪些章节
```

## P1 产品目标

让演示人员可以这样讲：

```text
这不是单纯 OCR。系统从银行流水里识别了账户、期间、入账出账、经营净现金流、收入波动率、DSCR 和异常交易。
然后把流水和发票、纳税申报、工商资料做交叉比对，判断收入真实性、经营稳定性和偿债能力。
这些结果可以同步到尽调任务，并进入报告的“财务状况”“收入真实性核实”“主要风险分析”章节。
```

## 一、字段识别结果区增强

### 1. 顶部增加识别摘要卡

在字段识别 tab 顶部增加摘要区，建议用 `el-card` / `el-descriptions` / `el-tag` / `el-progress`：

```text
银行流水摘要.pdf
文件类型：银行流水
识别字段：18 项
平均置信度：90%+
状态：待确认
可同步章节：财务状况、收入真实性核实、主要风险分析
```

摘要里要体现当前文件的业务价值：

```text
用于核验：收入真实性、经营稳定性、偿债能力
```

### 2. 字段分组展示

不要只平铺字段。请根据字段 label 分组展示：

```text
基础信息
- 账户名称
- 开户行
- 账号
- 流水期间

流水规模
- 总入账金额
- 总出账金额
- 月均入账
- 月均出账
- 经营净现金流

经营稳定性
- 月收入标准差
- 收入波动率
- 最大单月入账

异常交易识别
- 短期大额进出账
- 整数规律交易
- 关联账户互转
- 临近授信异常转入

偿债能力
- 债务本息支出
- DSCR
```

如果当前文件不是银行流水，也可以默认使用：

```text
基础信息 / 经营信息 / 税务信息 / 待确认字段
```

不要影响其他文件的字段展示。

### 3. 字段置信度展示

每个字段仍需展示置信度，但样式要更克制：

- 用 `el-progress` 或轻量进度条
- 低于 80% 用 warning
- 低于 70% 用 danger
- 不要使用过粗、过亮的进度条

### 4. 关键指标解释

对银行流水字段中的关键指标增加解释说明，建议在分组卡底部加小字说明或 `el-tooltip`：

```text
收入波动率 = 月收入标准差 / 月均收入
DSCR = 经营净现金流 / 债务本息支出，低于 1 表示现金流覆盖不足
```

## 二、交叉比对结果区增强

### 1. 增加交叉比对总览

交叉比对 tab 顶部增加一组摘要卡：

```text
主体一致：通过
收入匹配度：91.7%
资料完整度：86%
现金流偿债：DSCR 0.81
人工复核：4 项
```

建议用 `el-card` + `el-tag`，不要大面积彩色块。

### 2. 分 4 个业务区块展示

把交叉比对内容整理成以下四块：

```text
1. 数据一致性验证
   el-table：项目 / 结果 / 详情 / 来源

2. 收入真实性核实
   展示：流水 vs 发票、流水 vs 纳税申报、发票 vs 纳税申报
   显示差异率，说明是否超过 30% 人工复核阈值

3. 银行流水经营分析
   展示：月均入账、月均出账、经营净现金流、收入波动率、DSCR、异常交易数

4. AI 初步判断与复核建议
   使用 el-alert 或卡片列表展示：
   - 收入数据整体可交叉验证
   - 波动率偏高，经营稳定性需关注
   - DSCR 0.81，现金流覆盖不足
   - 异常流水和关联账户往来建议人工复核
```

### 3. 明确 30% 差异阈值

在收入真实性核实区明确展示规则：

```text
长期差异 > 30% -> 人工复核原因
```

唐山 demo 当前差异：

```text
发票收入 vs 纳税申报：4.4%
银行流水 vs 发票收入：8.3%
银行流水 vs 纳税申报收入：4.1%
```

结论：

```text
未超过 30% 阈值，收入数据整体可交叉验证；但异常交易仍需复核。
```

## 三、同步到尽调/报告的业务提示

在右侧结果区底部或操作区上方，增加一个轻量提示：

```text
识别和比对结果可同步至：
财务状况、收入真实性核实、主要风险分析、附件清单
```

按钮仍保留原有：

```text
确认并同步
同步到尽调
```

不要破坏原有按钮事件。

## 四、UI / UX 风格要求

本轮不是全页面重构，但新增区域必须符合项目整体风格：

- 使用 Element Plus：`el-card`、`el-table`、`el-tag`、`el-alert`、`el-progress`、`el-descriptions`
- 使用 `token.css` 变量，不要大量硬编码颜色
- 和企业探查、企业监测、智能报告页面的密度保持一致
- 结果区要紧凑、专业、可扫描
- 不要营销大字，不要大渐变，不要强阴影
- 不要大量 emoji
- 不要改变侧边栏和全局布局

推荐视觉层级：

```text
字段识别 tab：
  摘要卡
  字段分组卡
  同步提示
  操作按钮

交叉比对 tab：
  总览卡
  数据一致性表格
  收入真实性核实
  银行流水经营分析
  AI 判断与复核建议
```

## 五、不要做的事

本轮禁止：

- 不要修改工作台、智能尽调、智能报告、企业探查、企业监测。
- 不要修改路由。
- 不要修改 `token.css`。
- 不要引入新依赖。
- 不要删除其他企业 demo 数据。
- 不要破坏上传/确认/同步到尽调的原有功能。
- 不要为了处理终端乱码大面积替换中文。
- 不要把整页改成完全不同布局。

## 六、验收步骤

完成后运行：

```bash
npm run build
```

并自查：

```text
1. 打开资料识别页面
2. 默认选中唐山物桥 + 银行流水摘要.pdf
3. 字段识别 tab 顶部有识别摘要
4. 字段按基础信息/流水规模/经营稳定性/异常交易/偿债能力分组
5. 能看到收入波动率和 DSCR 的解释
6. 点击交叉比对，有总览卡、收入真实性、银行流水经营分析、AI 判断
7. 能看到 30% 差异阈值规则
8. 能看到同步到尽调/报告章节的提示
9. 原有上传、确认、同步按钮仍可用
10. npm run build 通过
```

## 七、完成后汇报格式

请按以下格式汇报：

```text
资料识别 P1 结果区业务表达增强完成报告

1. 修改了哪些文件
2. 字段识别 tab 增强了哪些内容
3. 银行流水字段如何分组
4. 交叉比对 tab 增强了哪些业务区块
5. 是否展示 30% 差异阈值和 DSCR 解释
6. 是否保留原有上传/确认/同步功能
7. 是否未修改其他模块
8. npm run build 是否通过
```

