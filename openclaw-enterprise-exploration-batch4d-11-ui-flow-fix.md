# OpenClaw 提示词：企业探查首页与工作台 UI/流程快速修复

项目路径：`D:\demo\ai-copilot`

你是资深 Vue 前端工程师和 UI 工程师。请只做本提示词指定的小范围修复，不要重构企业探查业务，不要改路由结构，不要改 mock 数据结构，不要做乱码修复。所有中文文案必须保持浏览器现有可读中文，禁止把中文改成乱码。

## 先读这些文件

必须先读：
- `src/pages/EnterpriseDiagnosisListPage.vue`
- `src/pages/EnterpriseExplorationWorkspacePage.vue`
- `src/pages/ScreeningInitialPage.vue`
- `src/styles/tokens.css`

参考但不要修改：
- `src/pages/ScreeningInitialPage.vue`

## 本次定位

本次只修企业探查两个页面：

1. 企业探查首页：`EnterpriseDiagnosisListPage.vue`
2. 企业探查工作台：`EnterpriseExplorationWorkspacePage.vue`

产品定位保持不变：
企业探查是一个对话式企业数据探索 Agent。用户可以输入企业名称、税号或自然语言问题，系统识别企业、检查工商/司法/税票/流水/社保等数据覆盖，再根据问题打开左侧结构化结果。简单问题在对话里回答；报告、明细、风险诊断、证据链在左侧展示。

## 当前问题

1. 企业探查首页输入框、开始探查按钮、推荐操作 chip 的字体和样式没有和智能筛客首页对齐。
2. 企业探查过程态对话布局不稳定：AI 和用户气泡左右关系不清晰，用户气泡有时跑到同一侧。
3. 过程态会卡在一个很短的“已”字，不继续输出。
4. 结果态右侧 AI 面板消息、操作按钮、输入区格式有错乱，局部按钮跑到气泡外面。
5. 右侧 AI 面板需要支持收起/展开；收起后左侧展示区要自动变宽，展开后恢复左右布局。
6. 样式需要和全局项目风格一致：使用 tokens.css 中的颜色、字号、圆角、边框，优先使用 Element Plus 组件。

## 目标效果

### 首页

- 输入框和“开始探查”按钮视觉对齐智能筛客首页的主输入区：
  - 大输入框，边框清晰，圆角、字号、间距一致。
  - 右侧主按钮高度和输入框协调，不要孤立过大或过小。
- 五组推荐问题仍然保留，不改内容：
  - 事实查询
  - 指标计算
  - 复杂分析
  - 报告生成
  - 证据链
- 推荐问题 chip 样式对齐智能筛客：
  - 浅蓝底、蓝色文字、胶囊形态、hover 有轻微反馈。
  - 分组标题字号、颜色、行距更克制，不要显得像表单标签。
- 首页说明文字减少压迫感，不新增大段说明。

### 过程态工作台

- 未打开左侧结构化结果前，保持“对话优先”的页面，不要白色大面板压住整页。
- AI 气泡固定在左侧，用户气泡固定在右侧。
- AI 头像使用蓝色 AI 圆形标识；用户头像用“我”浅蓝圆形标识。
- 对话气泡宽度合理：
  - AI 气泡最大宽度约 720px。
  - 用户气泡最大宽度约 520px。
  - 输入框宽度约 760px 到 920px，居中或跟对话主体对齐，不要过窄。
- 修复卡在“已”的问题：
  - 不允许只显示一个“已”字作为 AI 回复。
  - 识别企业后应输出完整句子，例如“已识别企业：唐山物桥商贸有限公司。当前数据覆盖：工商已获取、司法已获取、税票已获取、流水缺失、社保已获取。我将继续处理你的问题。”
  - 如果是流式模拟，至少保证最终文本完整。

### 结果态工作台

- 当打开左侧结构化结果后，采用左右布局：
  - 左侧：结构化结果、报告、明细、证据链。
  - 右侧：AI 探查助手。
- 右侧 AI 面板所有状态格式统一：
  - header：标题 + 收起按钮。
  - body：可滚动消息区。
  - footer：快捷操作 + 输入框。
- AI 消息在左，用户消息在右。
- 操作按钮必须归属于对应 AI 消息下方，不要漂浮到面板右侧或另一个气泡旁边。
- 收起 AI 面板后：
  - 右侧只保留窄条展开按钮。
  - 左侧内容区域自动扩展。
- 展开 AI 面板后：
  - 恢复固定右栏宽度，建议 360px 到 420px。

## 具体改法

### 1. 企业探查首页对齐智能筛客

在 `EnterpriseDiagnosisListPage.vue`：

- 保留现有 `chipGroups` 内容，不改中文文案。
- 参考 `ScreeningInitialPage.vue` 的输入区结构和视觉，不要直接复制业务逻辑。
- 可把首页输入区调整为类似：
  - 外层输入卡片
  - 左侧 icon
  - 中间 `el-input` 或原生 textarea/input
  - 右侧 `el-button type="primary"`
- chip 仍然点击后写入 `heroInput`，不自动提交。
- 不要新增页面级大卡片堆叠。

### 2. 修复过程态对话布局

在 `EnterpriseExplorationWorkspacePage.vue`：

- 检查 `chatMessages` 的渲染：
  - `msg.role === 'ai'` 使用左侧布局。
  - `msg.role === 'user'` 使用右侧布局。
  - `msg.type === 'engine'` 也归入 AI 左侧布局。
- 避免 avatar 和 bubble 使用同一套 flex 方向导致混乱。
- 建议增加或修正这些类：
  - `.ai-message`
  - `.ai-message--ai`
  - `.ai-message--user`
  - `.ai-message__avatar`
  - `.ai-message__bubble`
  - `.ai-message__actions`
- 用户气泡不允许出现在左侧；AI 气泡不允许出现在右侧。

### 3. 修复“已”卡住

在 `EnterpriseExplorationWorkspacePage.vue`：

- 定位 `runExploreFlow`、`sendChat`、模拟流式输出或分步输出的逻辑。
- 不要让中间态只写入单个字符。
- 如果有打字机效果，完成后必须把完整文本写回 message。
- `isExploring` 必须在流程结束后恢复为 `false`。
- 按钮文案从“探查中”恢复为“发送”。

### 4. 统一右侧 AI 面板

在 `EnterpriseExplorationWorkspacePage.vue`：

- 右侧 AI 面板统一为：
  - `.edw-ai-panel`
  - `.edw-ai-panel__header`
  - `.edw-ai-panel__body`
  - `.edw-ai-panel__footer`
- `body` 使用 `overflow-y: auto`，输入区固定在底部。
- 快捷操作按钮放在 AI 消息内部或 footer 的快捷区，不能漂浮。
- 收起/展开状态使用已有 `chatPanelCollapsed` 或新增一个明确 ref，不要出现未定义变量。

### 5. 样式统一要求

- 字号使用项目现有 token 或接近值：
  - 正文 13px/14px。
  - 标题 20px/24px，不要过大。
- 圆角以 6px/8px 为主。
- 主色使用现有蓝色，不要新增紫色、渐变、大面积阴影。
- 卡片边框使用浅蓝灰边框，背景用 `var(--surface-card)` 或项目已有浅色。
- 不要使用 emoji。
- 不要使用大段纯白面板覆盖浅灰页面背景。

## 禁止事项

- 不要修改路由。
- 不要修改 store 结构。
- 不要改 mock 数据结构。
- 不要把中文重新编码或批量替换。
- 不要删除现有企业探查能力。
- 不要改智能筛客页面。
- 不要引入新的 UI 框架。
- 不要联网下载资源。

## 验收标准

完成后运行：

```bash
npm run build
```

浏览器验收：

1. 企业探查首页输入框、开始探查按钮、推荐 chip 和智能筛客视觉一致。
2. 首页五组推荐问题仍在，内容未丢失。
3. 进入企业探查工作台后，AI 在左，用户在右。
4. 不再出现 AI 回复只显示“已”的卡住状态。
5. 过程态输入框宽度正常，不要过窄，不要漂浮。
6. 结果态右侧 AI 面板格式统一，消息和按钮不乱跑。
7. 右侧 AI 面板可以收起/展开，左侧内容区域会随之变宽或恢复。
8. 浏览器不出现新的中文乱码。
9. 控制台不出现 `ReferenceError` 或 Vue 未定义属性警告。

