# OpenClaw 提示词：Phase 3-A 智能尽调任务台账首页

请读取并严格执行本文件：

`openclaw-due-diligence-phase3a-task-ledger-prompt.md`

## 一、Phase 3 总体阶段规划

本阶段不是继续把工作台流程做成“一次性自动跑完”的演示，而是把「尽调任务」做成可持续推进的业务对象。

整体分 4 个阶段推进：

### Phase 3-A：智能尽调首页任务台账

目标：先把【智能尽调】首页改造成任务台账，让它既能承接工作台创建的任务，也能在本页直接新建尽调任务。

本阶段只做首页：

- 任务概览
- 任务筛选
- 任务列表
- 新建尽调弹窗
- 唐山物桥商贸有限公司 demo 任务
- 点击继续处理进入现有详情页

不要在本阶段重写详情页。

### Phase 3-B：智能尽调详情页任务工作区

目标：把详情页改造成“左侧阶段产物 + 右侧任务助手”的持续处理空间。

内容包括：

- 任务头部：企业、行业、区域、模板、来源、负责人、评分、资料完整度
- 流程进度：工商核验、司法查询、税票采集、资料补充、证据整合、风险诊断、产物确认
- 左侧阶段产物：尽量复用工作台已经沉淀的阶段产物组件
- 右侧任务助手：等待授权、等待资料、报告待确认等操作提示

### Phase 3-C：工作台与智能尽调 mock 同步

目标：工作台新建尽调后，智能尽调首页能看到同一个任务；智能尽调推进任务后，工作台能继续看到当前阶段。

内容包括：

- 共享 mock 任务对象 `DueDiligenceTask`
- 工作台创建任务写入同一任务列表
- 智能尽调首页识别来源：工作台 AI / 本页创建 / 筛客转入 / 企业探查
- 支持“回到工作台继续处理”

### Phase 3-D：智能尽调与智能报告打通

目标：产物确认后生成报告草稿，并同步到智能报告。

内容包括：

- 产物确认生成 `ReportDraft`
- 智能报告中能看到「唐山物桥商贸有限公司 尽职调查报告」
- 工作台和智能尽调只做轻量编辑入口
- 智能报告承担完整编辑、版本、导出

## 二、本次只做 Phase 3-A

请只实现 Phase 3-A：智能尽调首页任务台账。

## 三、重点修改文件

优先修改：

1. `src/pages/DueDiligenceHomePage.vue`
2. `src/stores/dueDiligence.js`
3. `src/data/mockDueDiligence.js`

必要时可少量调整：

4. `src/main.js` 中路由跳转如果已有则不要改

不要修改：

- `src/pages/DueDiligenceTaskPage.vue`
- `src/pages/WorkbenchPage.vue`
- `src/stores/workbenchAssistant.js`
- `src/components/workbench/*`
- `src/pages/SmartReportPage.vue`
- 任何独立业务模块页面

## 四、强制 UI 和样式要求

这次是主要 UI 改造，请严格使用 Element Plus 和项目全局 token。

必须使用 Element Plus 组件：

- `el-card`
- `el-button`
- `el-input`
- `el-select`
- `el-option`
- `el-tag`
- `el-table`
- `el-table-column`
- `el-progress`
- `el-dialog`
- `el-form`
- `el-form-item`
- `el-empty`
- `el-message`

禁止：

- 不要手写原生 `<table>` 做主任务列表
- 不要用大量自造图标
- 不要使用复杂渐变、厚重阴影、大圆角
- 不要写一堆硬编码颜色
- 不要新建一套视觉体系
- 不要改动全局 `tokens.css`，只允许引用 token

样式必须引用 `src/styles/tokens.css` 中已有变量，例如：

```css
var(--surface-page)
var(--surface-card)
var(--surface-soft)
var(--text-primary)
var(--text-secondary)
var(--text-tertiary)
var(--border-default)
var(--border-light)
var(--border-divider)
var(--color-primary)
var(--color-primary-bg)
var(--color-success)
var(--color-success-bg)
var(--color-warning)
var(--color-warning-bg)
var(--color-danger)
var(--color-danger-bg)
var(--radius-md)
var(--shadow-sm)
var(--space-md)
var(--space-lg)
var(--font-size-page-title)
var(--font-size-body)
```

如需 fallback，写成：

```css
background: var(--surface-card, #fff);
border: 1px solid var(--border-light, #e5eaf2);
```

## 五、智能尽调首页目标形态

请把智能尽调首页改造成如下结构：

```text
┌────────────────────────────────────────────────────────────────────────────┐
│ 智能尽调                                                                    │
│ 管理由工作台、筛客结果、企业探查或本页直接创建的尽调任务                       │
│                                                     [新建尽调] [从筛客创建] │
├────────────────────────────────────────────────────────────────────────────┤
│ 任务概览                                                                    │
│ ┌────────────┬────────────┬────────────┬────────────┬────────────┐          │
│ │ 进行中 8    │ 等待客户 3   │ 待确认 5    │ 已完成 12   │ 今日新增 2   │          │
│ └────────────┴────────────┴────────────┴────────────┴────────────┘          │
├────────────────────────────────────────────────────────────────────────────┤
│ 筛选区                                                                      │
│ 搜索企业/信用代码...  状态[全部 v] 来源[全部 v] 模板[全部 v]                 │
│ [全部] [进行中] [等待客户] [待确认] [已完成] [来自工作台] [本页创建]            │
├────────────────────────────────────────────────────────────────────────────┤
│ 任务列表                                                                    │
│ ┌────┬──────────────────┬────────────┬──────────┬──────────────┬─────┬────┐ │
│ │状态│ 企业              │ 模板        │ 来源      │ 当前阶段       │进度 │操作│ │
│ ├────┼──────────────────┼────────────┼──────────┼──────────────┼─────┼────┤ │
│ │等待│ 唐山物桥商贸有限...│ 尽职调查报告 │ 工作台AI   │ 税票采集/待授权 │43%  │继续│ │
│ │进行│ 杭州智造科技...    │ 尽职调查报告 │ 本页创建   │ 资料补充       │57%  │查看│ │
│ │待确│ 宁波天合新材料...  │ 尽职调查报告 │ 筛客转入   │ 报告待确认     │100% │编辑│ │
│ │完成│ 温州瑞达机械...    │ 尽职调查报告 │ 企业探查   │ 已完成         │100% │报告│ │
│ └────┴──────────────────┴────────────┴──────────┴──────────────┴─────┴────┘ │
└────────────────────────────────────────────────────────────────────────────┘
```

## 六、首页必须具备的交互

### 1. 新建尽调

点击「新建尽调」后打开 `el-dialog`：

```text
┌──────────────────────────────┐
│ 新建尽调                       │
├──────────────────────────────┤
│ 企业名称：唐山物桥商贸有限公司  │
│ 统一社会信用代码：91130203...   │
│ 尽调模板：[尽职调查报告 v]       │
│ 任务负责人：[张经理 v]           │
│ 优先级：[普通 v]                │
│                               │
│ [取消] [创建尽调任务]            │
└──────────────────────────────┘
```

表单字段：

- 企业名称：默认可选 `唐山物桥商贸有限公司`
- 统一社会信用代码：`91130203MA7EEQ2N0T`
- 尽调模板：默认 `尽职调查报告`
- 负责人：默认 `张经理`
- 优先级：默认 `普通`

点击「创建尽调任务」：

- 如果任务列表已有唐山物桥商贸有限公司，不重复创建，提示：`该企业已有尽调任务，已为你定位到任务列表`
- 如果没有，新增任务并置顶
- 弹出 `ElMessage.success('已创建尽调任务')`
- 关闭弹窗

### 2. 继续处理

任务列表中唐山物桥商贸有限公司默认存在一条 demo 任务：

```text
企业：唐山物桥商贸有限公司
模板：尽职调查报告
来源：工作台AI
当前阶段：税票采集 / 待授权
进度：43%
状态：等待客户
负责人：张经理
风险等级：中风险
资料完整度：67%
```

点击「继续」：

- 调用 store 的 `selectTask(task.id)`
- 跳转到 `/due-diligence/:taskId`
- 不要在本阶段重写详情页，只保证能进入现有详情页

### 3. 筛选与搜索

首页需要支持：

- 搜索企业名称 / 信用代码
- 按状态筛选：全部、进行中、等待客户、待确认、已完成
- 按来源筛选：全部、工作台AI、本页创建、筛客转入、企业探查
- 按模板筛选：全部、尽职调查报告、小微快审尽调、税票专项尽调

筛选后任务列表为空时，用 `el-empty`。

### 4. 任务概览

顶部统计卡片用 `el-card` 或统一卡片样式，展示：

- 进行中
- 等待客户
- 待确认
- 已完成
- 今日新增

统计必须从任务列表 computed 计算，不要硬写死。

## 七、数据结构要求

请在 `src/data/mockDueDiligence.js` 或 `src/stores/dueDiligence.js` 中确保任务对象包含以下字段。

```js
{
  id: 'dd-ts-wq-001',
  name: '唐山物桥商贸有限公司',
  creditCode: '91130203MA7EEQ2N0T',
  industry: '建材批发 / 商贸流通',
  region: '河北省唐山市',
  amount: '500万',
  templateName: '尽职调查报告',
  source: '工作台AI',
  manager: '张经理',
  priority: '普通',
  status: '等待客户',
  statusText: '税票采集 / 待授权',
  currentStage: 'taxCollection',
  currentStep: 'tax-rpa',
  progress: 43,
  riskLevel: '中风险',
  score: 72,
  grade: 'C+',
  materialCompleteness: 67,
  nextAction: '继续处理',
  updatedAt: '2026-07-02 15:40',
  reportDraftId: '',
}
```

如果现有代码使用 `currentStep`，请兼容现有字段，不要破坏详情页。

## 八、文本要求

页面中文必须正常显示，不要引入编码问题。请使用 UTF-8 保存文件。

页面文案：

```text
标题：智能尽调
副标题：管理由工作台、筛客结果、企业探查或本页直接创建的尽调任务
按钮：新建尽调、从筛客结果创建、继续处理、查看任务、编辑报告、查看报告
状态：进行中、等待客户、待确认、已完成
来源：工作台AI、本页创建、筛客转入、企业探查
模板：尽职调查报告
```

不要出现旧文案：

```text
标准授信尽调
尽调底稿
系统将自动完成全部流程
一次性自动生成全部产物
```

## 九、验收标准

完成后请验证：

1. `npm run build` 必须通过
2. `/due-diligence` 首页显示正常中文
3. 首页使用 Element Plus 组件，不再使用原生 `<table>` 作为主任务列表
4. 首页整体样式引用 `tokens.css` 变量
5. 任务列表有唐山物桥商贸有限公司 demo 任务
6. 唐山物桥任务显示：
   - 模板：尽职调查报告
   - 来源：工作台AI
   - 当前阶段：税票采集 / 待授权
   - 进度：43%
   - 状态：等待客户
7. 点击「新建尽调」打开 Element Plus 弹窗
8. 点击「创建尽调任务」可创建或定位唐山物桥任务，不重复创建
9. 点击「继续处理」可以进入现有 `/due-diligence/:taskId` 详情页
10. 没有修改工作台、智能报告、独立业务模块页面

## 十、输出报告格式

完成后请输出：

```text
Phase 3-A 智能尽调首页任务台账完成报告

1. 修改了哪些文件
2. 首页现在包含哪些模块
3. 是否保留智能尽调直接新建任务入口
4. 唐山物桥商贸有限公司 demo 任务是否已加入
5. 任务列表是否使用 Element Plus el-table
6. 样式是否使用 token.css 变量
7. 点击继续处理是否进入详情页
8. npm run build 是否通过
9. 是否修改了工作台或智能报告等非本阶段模块
```

