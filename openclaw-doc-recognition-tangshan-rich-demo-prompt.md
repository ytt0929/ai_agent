# OpenClaw 提示词：资料识别模块唐山物桥 Demo 丰富化 + Element Plus/token.css 规范

请读取并修改资料识别模块相关代码。本轮目标是把 `/doc-recognition` 或当前「资料识别」页面做成更真实的 demo：页面 UI 使用 Element Plus 组件 + 项目 `token.css` 变量；唐山物桥商贸有限公司作为主 demo 企业，字段识别、交叉比对、银行流水经营分析要更丰富，体现资料识别不仅提取字段，还能服务尽调模板中的收入真实性、经营稳定性、偿债能力判断。

## 重点文件

请优先查看并修改：

- `src/pages/DocRecognitionPage.vue`
- `src/stores/docRecognition.js`

必要时可只补充当前模块自己的 mock 数据。不要改工作台、智能尽调、智能报告、企业探查、企业监测等其他模块。

## 当前问题

现在资料识别页面已经有三栏：

```text
左侧：企业/任务列表
中间：文件上传与文件列表
右侧：字段识别 / 交叉比对
```

但问题是：

1. 右侧字段识别偏基础，只有营业执照、纳税申报、发票等浅层字段。
2. 唐山物桥商贸有限公司的 `银行流水摘要.pdf` 仍是识别中，无法体现银行流水在尽调中的价值。
3. `mockCrossCompare` 里没有针对唐山任务 `dd-ts-wq` 的丰富交叉比对结果。
4. 交叉比对没有充分体现：
   - 收入真实性核实
   - 流水与发票/纳税/财报匹配度
   - 经营稳定性趋势
   - 偿债能力 DSCR
5. UI 有不少自定义 div/chip/table，需收敛到 Element Plus + token.css 风格。

## 业务需求：银行流水在尽调模板中的应用

请把以下业务规则沉到唐山物桥 demo 的字段识别和交叉比对中：

### 1. 核实收入真实性

银行流水主要用于识别：

- 短期大额进出账冲量
- 机刷规律性整数交易
- 关联账户互转
- 临近授信异常转入

### 2. 核验流水与收入匹配度

对比维度：

```text
银行流水 vs 发票 vs 纳税申报 vs 财报/经营报表
```

示例规则：

```text
长期差异 > 30% -> 人工复核原因
```

### 3. 核验经营稳定性

常见指标：

```text
月均入账
月均出账
收入波动率 = 月收入标准差 / 月均收入
```

### 4. 核验偿债能力

指标：

```text
经营净现金流 = 流入 - 流出
DSCR = 经营净现金流 / 债务本息支出
```

解释：

```text
如 DSCR < 1，则说明现金流不足以覆盖债务本息，存在违约风险。
```

## 唐山物桥 demo 数据要求

### 1. 默认任务

资料识别页面打开后，建议默认选中：

```text
唐山物桥商贸有限公司
```

也就是 `currentTaskId` 应为 `dd-ts-wq`，默认选中文件可以是：

```text
银行流水摘要.pdf
```

如果为了保持现有交互，也可以默认选中营业执照，但必须保证点击「交叉比对」后展示唐山物桥的完整分析结果。

### 2. 文件列表

唐山物桥至少包含以下文件，名称尽量用中文真实名称：

```text
营业执照.pdf
纳税申报表.xlsx
增值税发票.pdf
发票明细.xlsx
企业所得税.pdf
纳税评级.pdf
银行流水摘要.pdf
工商登记信息.pdf
```

建议将 `银行流水摘要.pdf` 状态从「识别中」改为「待确认」或「已完成」，这样 demo 可以直接看到识别结果。

### 3. 银行流水字段识别

为 `银行流水摘要.pdf` 增加字段识别 mock，例如：

```text
账户名称：唐山物桥商贸有限公司
开户行：中国民生银行唐山分行
账号：6222 **** **** 8791
流水期间：2025-01 至 2025-12
总入账金额：2,086.32万元
总出账金额：1,934.76万元
月均入账：173.86万元
月均出账：161.23万元
经营净现金流：151.56万元
月收入标准差：72.40万元
收入波动率：41.6%
最大单月入账：318.20万元
短期大额进出账：4笔
整数规律交易：12笔
关联账户互转：3笔
临近授信异常转入：2笔
债务本息支出：186.00万元
DSCR：0.81
```

字段置信度建议：

- 主体、账户、期间、总入账、总出账：90% 以上
- 异常交易类字段：75%-88%
- DSCR/波动率等派生指标：80%-90%

### 4. 唐山交叉比对结果

请为 `dd-ts-wq` 增加 `mockCrossCompare.dd-ts-wq`，至少包含：

#### 数据一致性验证

```text
企业主体一致
营业执照、纳税申报、发票明细、银行流水均指向唐山物桥商贸有限公司

统一社会信用代码一致
营业执照 = 纳税申报 = 工商登记 = 91130203MA7EEQ2N0T

发票收入 vs 纳税申报
发票收入 2275.98 万元，纳税申报收入 2175.46 万元，差异 4.4%

银行流水 vs 发票收入
银行流水入账 2086.32 万元，发票收入 2275.98 万元，差异 8.3%

银行流水 vs 纳税申报收入
银行流水入账 2086.32 万元，纳税申报收入 2175.46 万元，差异 4.1%

流水异常交易
短期大额进出账 4 笔，整数规律交易 12 笔，关联账户互转 3 笔，临近授信异常转入 2 笔

偿债能力
经营净现金流 151.56 万元，债务本息支出 186 万元，DSCR 0.81，低于 1
```

#### 经营指标分析

至少包含这些指标卡：

```text
月均入账：173.86万元
月均出账：161.23万元
经营净现金流：151.56万元
收入波动率：41.6%
流水发票匹配度：91.7%
流水纳税匹配度：95.9%
DSCR：0.81
短期冲量交易：4笔
整数规律交易：12笔
关联互转交易：3笔
临近授信异常转入：2笔
```

#### AI 初步判断

请给出更像尽调辅助的结论：

```text
综合银行流水、发票、纳税申报和工商资料，唐山物桥商贸有限公司主体资料一致，收入数据整体可交叉验证，但现金流偿债覆盖不足，且存在若干异常交易信号，建议人工复核异常流水和关联账户往来。
```

要点：

- 收入真实性：流水与发票差异 8.3%，未超过 30%，整体可验证，但需复核异常交易。
- 经营稳定性：收入波动率 41.6%，波动偏高。
- 偿债能力：DSCR 0.81，现金流覆盖债务本息不足。
- 异常交易：短期大额进出账、整数规律交易、关联账户互转、临近授信异常转入需人工复核。

## UI 改造要求

### 总原则

页面整体要使用 Element Plus + token.css：

- 容器：`el-card`
- 筛选：`el-segmented` 或 `el-radio-group`
- 搜索：`el-input`
- 文件列表：可保留卡片，但按钮/tag 用 `el-tag`、`el-button`
- 字段识别：建议用 `el-descriptions` / `el-table` / `el-progress`
- 交叉比对：建议用 `el-tabs` 或保留现有 tab，但内容用 `el-card`、`el-table`、`el-tag`、`el-alert`、`el-progress`
- 指标卡：`el-card` + token spacing，不要大面积硬编码颜色

必须引用项目 token 变量：

```css
var(--surface-card)
var(--bg-page)
var(--border-default)
var(--border-light)
var(--text-primary)
var(--text-secondary)
var(--text-tertiary)
var(--color-primary)
var(--color-success)
var(--color-warning)
var(--color-danger)
var(--radius-md)
var(--space-sm/md/lg)
var(--font-size-sm/base/lg)
```

如果当前项目 token 命名略有差异，请使用现有 `src/styles/tokens.css` 中已定义变量，不要新增全局 token。

### 必须与项目整体风格一致

本页面不能做成孤立的新风格。请对齐当前项目中已经改造过的页面风格，尤其参考：

- 企业探查首页/工作区的卡片边界、背景色、字体层级
- 企业监测首页的列表卡片密度和 tag 风格
- 智能报告首页/模板中心的 Element Plus 表格、筛选区、按钮风格

统一要求：

```text
页面背景：使用项目浅灰工作区背景，不要纯白铺满
主内容容器：使用 el-card 或等价卡片，边框细、阴影弱或无阴影
卡片圆角：跟随 token，不要大圆角
标题层级：页面标题、区块标题、表格标题要和其他模块一致
字体大小：不要放大到营销页尺寸，使用管理后台密度
按钮：主按钮用 Element Plus primary，次按钮 plain/text/link，避免自定义大色块
标签：使用 el-tag 的 success/warning/danger/info，不要自造彩色胶囊
表格：使用 el-table，表头、行高、边框和智能报告/企业探查保持一致
间距：使用 var(--space-sm/md/lg)，不要写大量 20/24/32 的孤立数值
图标：使用 Element Plus 图标即可，不要大量 emoji 或自定义图标
滚动：页面主体与三栏内部滚动要稳定，不要出现浏览器外层和内部同时乱滚
```

三栏布局也要对齐整体产品的“业务工具页”气质：

```text
左栏：企业/任务列表，宽度稳定，卡片密度紧凑
中栏：上传区 + 文件列表，上传区不要过高，文件列表可滚动
右栏：字段识别/交叉比对结果区，顶部 tab 固定，内容区滚动
```

不要使用以下风格：

- 大面积渐变背景
- 大量 emoji 图标
- 强阴影浮层
- 过大的彩色指标卡
- 与当前侧边栏、企业探查、智能报告明显不一致的字体和颜色

### 交叉比对展示建议

右侧「交叉比对」不只是列表，要分 4 块：

```text
┌──────────────────────────────┐
│ 交叉比对总览                  │
│ 主体一致  资料完整度  流水匹配度 │
└──────────────────────────────┘

┌──────────────────────────────┐
│ 数据一致性验证                │
│ el-table: 项目 / 状态 / 详情 / 来源 │
└──────────────────────────────┘

┌──────────────────────────────┐
│ 银行流水经营分析              │
│ 月均入账 | 月均出账 | 波动率 | DSCR │
│ 异常交易：短期冲量/整数交易/关联互转 │
└──────────────────────────────┘

┌──────────────────────────────┐
│ AI 初步判断                   │
│ 收入真实性 / 经营稳定性 / 偿债能力 / 人工复核建议 │
└──────────────────────────────┘
```

### 字段识别展示建议

字段识别需要支持不同文件的字段差异，尤其银行流水：

```text
营业执照字段：
企业名称、统一社会信用代码、法定代表人、注册资本、成立日期、经营范围

银行流水字段：
账户名称、开户行、账号、流水期间、总入账、总出账、月均入账、月均出账、经营净现金流、波动率、DSCR、异常交易数
```

建议用字段分类：

```text
基础信息
财务/流水指标
异常交易识别
偿债能力
```

如果不想做复杂分组，至少字段列表要足够丰富，且展示置信度。

## 交互验收

完成后请自查：

```text
1. 打开资料识别页面
2. 默认能看到唐山物桥商贸有限公司任务
3. 点击银行流水摘要.pdf
4. 字段识别能看到银行流水的丰富字段
5. 点击「交叉比对」
6. 能看到收入真实性、匹配度、经营稳定性、偿债能力、异常交易和 AI 判断
7. 页面使用 Element Plus 组件和 token.css 变量，视觉不应出现大面积自定义硬编码风格
8. 控制台无报错
```

## 禁止事项

本轮严禁：

- 不要修改工作台、智能尽调、智能报告、企业探查、企业监测。
- 不要修改路由。
- 不要修改 `token.css`。
- 不要引入新依赖。
- 不要为了处理终端乱码大面积替换中文内容。
- 不要删除现有其他企业 demo 数据。
- 不要破坏上传/确认/同步到尽调的原有按钮功能。

## 构建与汇报

完成后运行：

```bash
npm run build
```

请按以下格式汇报：

```text
资料识别唐山物桥丰富 Demo 完成报告

1. 修改了哪些文件
2. 唐山物桥新增/调整了哪些文件和字段识别结果
3. 银行流水经营分析包含哪些指标
4. 交叉比对新增了哪些一致性验证和 AI 判断
5. UI 如何使用 Element Plus + token.css
6. 是否未修改其他模块
7. npm run build 是否通过
```
