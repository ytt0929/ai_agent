# OpenClaw 提示词：企业监测新增监控弹窗 Demo 自然语言识别增强

请读取并修改企业监测相关代码。本轮只增强 `/enterprise-monitor` 页面中「新增监控任务」弹窗的 demo 自然语言识别体验，不改变现有业务列表、筛选、详情抽屉、预警处理流程，不做大范围样式重构，不处理编码/乱码。

## 背景目标

当前「新增监控任务」弹窗里有“自然语言”模式和“AI 识别”按钮，但 demo 阶段输入框为空，操作者不知道输入什么，也看不出 AI 识别能力。

需要改成：打开弹窗后，默认填入一段面向 **唐山物桥商贸有限公司** 的自然语言监控要求，操作者可以直接点击「AI 识别」，系统自动识别企业主体、统一社会信用代码和监控指标，并展示结构化识别结果。

## 需要重点修改的文件

请优先查看：

- `src/pages/EnterpriseMonitorPage.vue`
- `src/stores/enterpriseMonitor.js`
- `src/data/mockEnterpriseMonitor.js`

如果实际项目中企业监测拆分到了其他组件，请沿着 import/emit/store 调用关系定位，但不要改其他模块。

## Demo 默认自然语言文案

打开「新增监控」弹窗时，自然语言输入框默认填入：

```text
监控唐山物桥商贸有限公司，重点关注税负率显著低于行业、开票收入连续下降或异常波动、新增被执行或诉讼、法人股东工商变更、经营异常名录；一旦触发高风险请提醒我。
```

要求：

1. 文案默认出现在 textarea 中，不是 placeholder。
2. 用户可以直接点击「AI 识别」。
3. 用户仍然可以清空并输入其他文本，不要写死不可编辑。

## AI 识别结果要求

点击「AI 识别」后，识别结果至少包含：

```text
企业：唐山物桥商贸有限公司
统一社会信用代码：91130203MA7EEQ2N0T

识别到的监控指标：
1. 税负率异常
   条件：税负率显著低于行业均值或连续下降
   等级：高风险

2. 开票收入波动
   条件：开票收入连续下降或异常波动
   等级：高风险

3. 司法风险
   条件：新增被执行、诉讼、裁判文书或失信记录
   等级：高风险

4. 工商变更
   条件：法人、股东、经营范围或注册地址发生变更
   等级：中风险

5. 经营异常
   条件：新增经营异常名录、行政处罚或严重违法记录
   等级：中风险
```

## 解析逻辑要求

请增强 `parseMonitorText(text)` 或对应解析函数：

1. 能识别以下输入中的唐山企业：
   - `唐山物桥商贸有限公司`
   - `91130203MA7EEQ2N0T`
   - 包含“唐山物桥”的自然语言句子

2. 识别到唐山企业时，返回：

```js
{
  enterprises: ['唐山物桥商贸有限公司'],
  creditCode: '91130203MA7EEQ2N0T',
  indicators: [...]
}
```

如果当前数据结构没有 `creditCode` 字段，请补充，并在创建任务时优先使用 parsed.creditCode。

3. 指标关键词映射建议：

```text
税负率 / 税负 / 税票 / 纳税 / 税务
  -> 税负率异常 / 税票波动

开票 / 收入 / 营收 / 异常波动 / 连续下降
  -> 开票收入波动

被执行 / 诉讼 / 司法 / 裁判 / 失信
  -> 司法风险

法人 / 股东 / 工商 / 经营范围 / 注册地址
  -> 工商变更

经营异常 / 行政处罚 / 严重违法
  -> 经营异常
```

4. 去重：同一指标不要重复出现。
5. 如果没有识别到任何指标，再使用兜底指标，但唐山 demo 默认文案必须识别出上述 5 类。

## UI 展示要求

在「AI 已识别」区域中，结构要清晰体现 AI 识别能力：

```text
AI 已识别
企业主体：唐山物桥商贸有限公司
统一社会信用代码：91130203MA7EEQ2N0T
识别来源：自然语言

监控指标
[高风险] 税负率异常     税负率显著低于行业均值或连续下降
[高风险] 开票收入波动   开票收入连续下降或异常波动
[高风险] 司法风险       新增被执行、诉讼、裁判文书或失信记录
[中风险] 工商变更       法人、股东、经营范围或注册地址发生变更
[中风险] 经营异常       新增经营异常名录、行政处罚或严重违法记录

[确认创建]
```

样式要求：

- 使用现有 Element Plus 组件：`el-input`、`el-button`、`el-tag`、`el-card`、`el-descriptions`、`el-alert` 等。
- 使用项目现有 token.css 变量，不新增大面积硬编码颜色。
- 保持当前弹窗尺寸基本不变，必要时可以让识别结果区域内部滚动。
- 不要使用很多 emoji 或自造 icon，保持企业监测现有克制风格。

## 创建任务后的数据要求

点击「确认创建」后，新建的任务需要：

```text
enterpriseName: 唐山物桥商贸有限公司
creditCode: 91130203MA7EEQ2N0T
source: natural-language
sourceLabel: 自然语言
status: running
indicators: 上面识别出的 5 条监控指标
warningCount: 0 或 demo 可保留 1-3，但不要破坏现有列表
```

如果 `mockEnterpriseMonitor.js` 中已经存在唐山物桥的任务，不要删除。新建 demo 任务可以插入列表顶部，让操作者看到创建结果。

## 交互细节

1. 点击「新增监控」：
   - 默认停留在自然语言 tab。
   - textarea 默认填入唐山物桥的 demo 文案。
   - 「AI 识别」按钮可直接点击。

2. 点击「AI 识别」：
   - loading 约 400-800ms 即可。
   - 出现结构化识别结果。
   - 不要自动创建任务，必须用户点击「确认创建」。

3. 点击「确认创建」：
   - 保持原有创建中步骤动画。
   - 关闭弹窗后，列表顶部出现新任务。
   - 可以用 `ElMessage.success('已创建唐山物桥商贸有限公司监控任务')` 提示。

## 禁止事项

本轮不要做以下事情：

- 不要改企业监测首页列表结构和筛选逻辑。
- 不要改工作台、智能筛客、企业探查、智能尽调、智能报告等模块。
- 不要修改 `token.css`。
- 不要做全局样式重构。
- 不要为了处理终端乱码大面积替换中文。
- 不要引入新依赖。
- 不要改路由。

## 验收路径

完成后请运行：

```bash
npm run build
```

然后按以下流程自查：

```text
1. 打开 /enterprise-monitor
2. 点击「新增监控」
3. 弹窗默认在「自然语言」tab
4. textarea 已默认填入唐山物桥 demo 文案
5. 直接点击「AI 识别」
6. 能看到企业主体、统一社会信用代码和 5 条监控指标
7. 点击「确认创建」
8. 列表顶部出现唐山物桥商贸有限公司的新监控任务，来源为「自然语言」
9. 控制台无报错
```

## 完成后汇报格式

请按以下格式回复：

```text
企业监测自然语言 Demo 增强完成报告

1. 修改了哪些文件
2. 默认自然语言文案是什么
3. AI 识别出了哪些企业信息和指标
4. 创建任务后列表数据如何变化
5. 是否未修改其他模块
6. npm run build 是否通过
```

