# OpenClaw 提示词：工作台 Phase 2 左侧产物内容增强

现在开始【工作台 Phase 2：左侧结果区产物内容增强与尽调展示统一】。

Phase 1 已完成状态机和主链路骨架，构建通过，但最终浏览器链路验收等 Phase 2 内容补齐后再统一做。  
本阶段不要继续安装 Playwright，不要写自动化测试，不要做完整浏览器链路验收，只做代码实现和 `npm run build`。

## Phase 2 总目标

工作台是 AI Copilot 的自然语言入口，不跳转页面。用户从工作台发起筛客、企业探查、新建尽调后，左侧结果区要逐步展示每个业务节点的真实产物内容，右侧 AI 对话只负责解释、确认和驱动下一步。

核心原则：

```text
左侧 = 业务结果 / 节点产物 / 可查看内容
右侧 = AI 对话 / 状态说明 / 下一步确认
顶部流程条 = 只读进度展示，不自由跳转
```

## 修改范围

只允许修改工作台相关文件：

```text
src/stores/workbenchAssistant.js
src/pages/WorkbenchPage.vue
src/components/workbench/WorkbenchBusinessPanel.vue
src/components/workbench/WorkbenchStageStrip.vue
src/components/workbench/artifacts/*.vue
```

可以新增工作台 artifact 子组件：

```text
src/components/workbench/artifacts/*.vue
```

不要修改独立业务模块页面和 store：

```text
src/pages/DueDiligenceTaskPage.vue
src/pages/DueDiligenceHomePage.vue
src/pages/EnterpriseExplorePage.vue
src/pages/SmartReportPage.vue
src/pages/TaxRpaPage.vue
src/pages/DocRecognitionPage.vue
src/stores/dueDiligence.js
src/stores/smartReport.js
src/stores/enterprise*.js
src/stores/tax*.js
```

如果需要参考独立页面内容，只能在工作台 artifact 中复制/模拟必要展示数据，不要改原页面。

## 需要实现的内容

### 1. 工作台二级页左侧统一成“尽调工作区”感觉

当流程进入新建尽调后，左侧结果区要更像智能尽调任务页的左侧区域，但不能跳转到智能尽调页面。

左侧应包含：

```text
企业头部信息
当前尽调模板
只读流程进度条
当前节点产物
下一步提示或待确认项
```

企业头部建议展示：

```text
唐山物桥商贸有限公司
建材批发 / 商贸流通 / 河北唐山 / 500万
综合评分 72 / C+ / 中风险 / 资料完整度 86%
```

如果当前还在筛客/企业探查阶段，不强行展示完整尽调头部，只保持现有筛客/探查结果。

### 2. 每个节点左侧产物要丰富，不要只有简单表格

请逐个增强以下 artifact 组件。要求风格保持工作台现有字体、间距、Element Plus 组件风格，不要大量 icon，不要新 UI 库。

#### 2.1 工商核验 BusinessVerifyArtifact

左侧展示内容：

```text
工商核验结果
- 主体状态：正常存续
- 注册资本：500万
- 成立时间：2018-03-15
- 行业：建材批发 / 商贸流通
- 法定代表人：张三
- 关联企业：3家
- 司法风险：无重大诉讼
- 税务评级：A 级
```

建议结构：

```text
状态摘要条
基础信息表格
核验结果列表
风险提示小卡
```

#### 2.2 司法查询 JudicialArtifact

左侧展示内容：

```text
司法查询结果
- 诉讼案件：0
- 被执行：0
- 失信记录：0
- 裁判文书：1
- 行政处罚：0
```

需要有一个轻量列表：

```text
查询项 / 结果 / 风险等级 / 说明
```

不要堆复杂图标。

#### 2.3 税票采集 TaxCollectionArtifact

税票采集要体现两个状态：

```text
未授权 / 等待授权
已授权 / 采集完成
```

等待授权时展示：

```text
授权状态：等待授权
链接状态：已发送 / 待发送
授权链接
二维码占位区域
采集进度：0/150、0/120、未采集
采集日志
```

采集完成时展示：

```text
授权状态：已授权
链接状态：已使用
进项发票：128/150 85%
销项发票：96/120 80%
纳税申报：已采集
采集日志
下一步：进入资料补充
```

注意：税票阶段不能自动跳过用户确认。必须保留：

```text
确认发送采集链接
模拟企业已授权
进入资料补充
改为上传材料
```

#### 2.4 资料补充 MaterialsArtifact

资料补充要模拟“资料包 + 识别结果”。

等待上传时展示：

```text
资料完整度：67%
已收集：营业执照、近一年纳税申报、开票明细、基础工商资料
缺失：主要合同、银行流水、购销说明、税负异常说明
动作：发送资料清单 / 模拟企业上传资料
```

上传完成后展示：

```text
资料完整度：86%
资料包清单
OCR 识别状态
待确认资料：购销说明、税负异常说明
下一步：进入证据整合
```

#### 2.5 证据整合 EvidenceMergeArtifact

证据整合要展示“证据来源 + 风险事项证据链”。

内容：

```text
资料完整度：86%
证据来源：
- 工商数据
- 司法数据
- 税票数据
- 上传资料

风险事项证据链：
- 开票收入与申报收入不一致：税票数据 + 申报表
- 税负率显著低于行业：纳税申报 + 行业指标
- 采购两头在外：合同资料 + 开票明细
- 短期偿债压力较大：银行流水缺失 + 财务资料不足

证据缺口：
- 银行流水仍需补充
- 税负异常说明待确认
```

#### 2.6 风险诊断 RiskDiagnosisArtifact

风险诊断要参考企业探查里的诊断报告效果，但保持工作台风格。

内容：

```text
综合评分：72
等级：C+
风险等级：中风险
授信建议：有条件授信

核心风险和亮点：
Tab：风险事项 / 企业亮点 / 全量指标
风险筛选：全部 / 高风险 / 中风险 / 低风险

风险事项列表：
- 营收增长异常
- 购销两头在外
- 短期偿债压力过大
- 应收账款周转率下降
- 税负率显著低于行业
- 开票收入与申报收入不一致
- 电费与收入相关性低
- 公司成立时间较短
```

每条风险展示：

```text
风险名称
风险等级
风险分类
说明
查看证据链
```

注意：不要把企业探查页面整页搬进来，只复制适合工作台左侧的核心内容。

#### 2.7 产物确认 DeliverablesArtifact

产物确认要展示“报告模板 + 资料包 + 待确认项”。

内容：

```text
产物已生成
报告模板：标准授信尽调报告
资料包：工商资料、司法查询、税票数据、上传资料、证据链
待确认项：
- 风险结论需确认
- 税票异常说明待补充
- 授信建议待确认

动作：
编辑报告
导出报告
加入监控
```

如果有 `exportStatus`，展示导出状态。

#### 2.8 报告编辑 ReportEditorArtifact

报告编辑不跳转页面。左侧切换成“报告编辑模式”。

结构：

```text
报告编辑
左侧小目录：
- 一 企业概况
- 二 工商核验
- 三 税票分析
- 四 风险诊断
- 五 授信建议

右侧正文：
标题 / 状态 / 资料完整度
章节正文
本章证据链
关联资料
待确认项
```

右侧 AI 对话保持存在，用于：

```text
改写风险结论
补充税票说明
生成授信建议
导出最终报告
```

不要跳转到 `SmartReportPage.vue`。

### 3. Store 中补齐每个阶段的 mock 数据

文件：

```text
src/stores/workbenchAssistant.js
```

要求：

- 唐山物桥商贸有限公司必须贯穿所有节点。
- 每个 `runXxxStep()` 生成的 `artifactData` 要满足上面 artifact 展示需要。
- 不要让 artifact 组件自己硬编码全部业务数据；核心 mock 数据应由 store 下发。
- 可以在组件中提供兜底默认值，但主流程数据来自 store。

唐山物桥统一数据：

```js
{
  name: '唐山物桥商贸有限公司',
  industry: '建材批发',
  segment: '商贸流通',
  location: '河北唐山',
  capital: '500万',
  score: 72,
  grade: 'C+',
  riskLevel: '中风险',
  completeness: 86
}
```

### 4. 右侧 AI 对话不要塞大过程卡片

右侧只做：

```text
状态说明
下一步建议
用户确认按钮
简短解释
```

大表格、大卡片、大过程图全部放左侧结果区。

如果现在右侧 `WorkbenchConversation` 仍展示流程卡片或大过程图，请不要扩展它。本阶段只保证新增内容进入左侧 artifact，不要把内容塞入右侧。

### 5. 顶部流程条保持只读

顶部流程条只展示流程进度，不允许点击自由跳转。  
如果当前还可点击切换 stage，请禁用点击。  
Phase 2 也不要让顶部流程条改变：

```text
currentFlowStatus
waitingForInput
contextSuggestions
```

## 验收标准

运行：

```bash
npm run build
```

并做代码层自查：

```text
每个 artifact 组件都有非空内容
每个节点 artifactData 能支撑页面展示
右侧没有新增大过程卡片
顶部流程条只读
独立业务页面没有被修改
```

本阶段可以不做完整浏览器自动化验收。不要安装 Playwright。

## 输出要求

完成后输出：

1. 修改了哪些文件。
2. 每个节点左侧新增/增强了哪些内容。
3. 是否修改了独立业务模块，如果没有请明确说明。
4. `npm run build` 是否通过。
5. 哪些内容留到 Phase 3 做真实浏览器链路验收。

