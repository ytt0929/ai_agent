# OpenClaw 提示词：资料识别 P2 页面整体 UI 收口

## 背景

资料识别模块 P0/P1/P1.2 已经完成：

- 默认演示态：唐山物桥商贸有限公司 + 银行流水摘要.pdf
- 字段识别：已支持字段分组、置信度、DSCR、收入波动率说明
- 交叉比对：已改为 AI 分析优先
- 业务内容已经足够支撑 demo

本阶段不要再扩展业务内容。  
本阶段只做页面整体 UI/UX 收口，让资料识别页面符合项目统一的 Element Plus + token.css 风格。

## 本次目标

优化【资料识别】页面整体布局和视觉一致性，让页面更像成熟工作台：

1. 三栏布局更稳定
2. 左侧企业列表更清晰
3. 中间文件区更规整
4. 右侧结果区更像业务分析面板
5. 滚动区域明确，不要整页混乱滚动
6. 空态、选中态、待确认态、已完成态更一致
7. 不改变现有业务功能和数据

## 修改范围

优先只修改：

- `src/pages/DocRecognitionPage.vue`

如存在局部样式复用组件，可以小范围调整该页面相关 scoped CSS。

禁止修改：

- `src/stores/docRecognition.js` 的业务数据和字段含义
- 工作台
- 智能尽调
- 智能报告
- 企业探查
- 企业监测
- 路由
- `src/styles/token.css`
- 其他模块 mock 数据

## 布局要求

页面继续保持三栏结构：

```text
┌──────────────┬──────────────────────┬────────────────────────────┐
│ 企业/文件筛选 │ 文件上传与文件列表       │ 字段识别 / 交叉比对结果区      │
│              │                      │                            │
│ 企业列表       │ 当前企业标题             │ tab：字段识别 / 交叉比对        │
│ 搜索/筛选      │ 上传区域                 │ 摘要卡 / 分组字段 / 分析区      │
│ 状态标签       │ 文件列表                 │ 确认同步操作                  │
└──────────────┴──────────────────────┴────────────────────────────┘
```

### 1. 页面外层

- 使用统一浅色背景，参考企业探查、企业监测、智能报告已改造后的风格
- 主内容区宽度不要无限铺满，使用合理的 `max-width` 或稳定 grid
- 页面高度应适配视口，避免内容被浏览器底部裁切
- 需要有明确滚动区：
  - 企业列表可以独立滚动
  - 文件列表可以独立滚动
  - 右侧结果区可以独立滚动

### 2. 左侧企业列表区

- 使用 `el-card` 或等价 Element Plus 卡片风格
- 搜索框使用 `el-input`
- 筛选按钮优先使用 `el-segmented` 或 `el-radio-group`
- 企业项选中态要清晰，但不要大面积深色
- 企业项内展示：
  - 企业名称
  - 行业/地区/规模
  - 文件数量
  - 状态点或 `el-tag`
- 唐山物桥商贸有限公司默认选中时要一眼可识别

### 3. 中间文件区

- 当前企业标题区使用清晰层级
- 上传区域使用 `el-upload` 或保留现有上传逻辑但视觉上 Element Plus 化
- 文件列表每一项使用轻量卡片/列表行：
  - 文件名
  - 文件类型/大小/时间
  - 状态 tag：已完成 / 待确认 / 识别中
- 选中文件要有明确背景和左侧强调线或边框
- 文件列表不要堆得太松，也不要过度拥挤

### 4. 右侧结果区

右侧结果区是核心，必须看起来像“业务分析面板”，不是普通空白详情页。

字段识别 tab：

- 顶部保留识别摘要卡
- 字段分组使用 `el-card` 或轻量分组块
- 字段项用统一行高和置信度展示
- `el-progress` 不要太粗，颜色使用 Element Plus 状态色或 token 色
- DSCR、收入波动率说明要保留，样式克制

交叉比对 tab：

- 顺序必须保持：
  1. AI分析与复核建议
  2. 收入真实性核实
  3. 银行流水经营分析
  4. 数据一致性验证
  5. 交叉比对总览
- 每个区块使用统一标题、统一卡片间距
- 不要新增业务内容
- 不要把“交叉比对总览”重新移回顶部

### 5. 底部操作

- `确认并同步`
- `同步到尽调`

按钮位置要稳定。可以固定在右侧结果区底部，也可以放在内容末尾，但不能遮挡内容。

## 样式要求

必须使用项目现有 Element Plus + token.css 风格：

- 优先使用：
  - `el-card`
  - `el-input`
  - `el-button`
  - `el-tag`
  - `el-tabs`
  - `el-table`
  - `el-alert`
  - `el-progress`
  - `el-descriptions`
  - `el-empty`
- CSS 颜色、边框、圆角、字体、间距优先引用 `src/styles/token.css` 已有变量
- 不要修改 `token.css`
- 不要新增大面积渐变
- 不要使用 emoji 图标
- 不要使用重阴影
- 不要使用孤立的新配色
- 不要把页面做成营销页
- 这是后台业务工具，应保持克制、清晰、密集但不拥挤

## 禁止事项

- 不要改业务数据
- 不要删掉唐山物桥 demo 数据
- 不要改默认选中的企业和文件
- 不要改字段识别 / 交叉比对的业务逻辑
- 不要改 tab 顺序
- 不要改其他模块
- 不要引入新依赖
- 不要因为中文显示问题去改编码或重写文案

## 验收标准

请完成后按以下路径自查：

1. 打开资料识别页面
2. 默认选中唐山物桥商贸有限公司
3. 默认选中银行流水摘要.pdf
4. 字段识别 tab 首屏不是空白
5. 交叉比对 tab 首屏第一块是 AI分析与复核建议
6. 三栏布局稳定，不互相挤压
7. 左侧企业列表、中间文件列表、右侧结果区滚动明确
8. 页面风格和企业探查、企业监测、智能报告保持一致
9. 上传、确认并同步、同步到尽调功能保留
10. 浏览器控制台无 Vue warning / error
11. `npm run build` 通过

## 完成后请汇报

请明确汇报：

1. 修改了哪些文件
2. 是否只做 UI/UX 收口，未改业务数据
3. 三栏滚动区域如何处理
4. 字段识别 tab 是否保持原有内容
5. 交叉比对 tab 顺序是否保持 AI 分析优先
6. 是否保留上传、确认并同步、同步到尽调功能
7. 是否未修改其他模块
8. `npm run build` 是否通过
