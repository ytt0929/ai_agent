# OpenClaw 提示词：资料识别 P0 默认演示态修复

请读取资料识别模块相关代码，并执行本轮 P0 修复。本轮只解决「资料识别页面打开后右侧空白、demo 不好演示」的问题，不做大范围 UI 重构，不改其他模块。

## 目标

资料识别页面打开后，应直接进入一个可演示状态：

```text
默认选中：唐山物桥商贸有限公司
默认文件：银行流水摘要.pdf
右侧面板：直接展示字段识别结果
交叉比对 tab：有唐山物桥的交叉比对摘要，不再空白
```

核心是让演示人员进入页面后，不需要先猜该点哪个文件，就能直接讲：

```text
这是一份唐山物桥的尽调资料包。
系统已经识别了银行流水摘要。
可以看到字段提取、置信度、交叉比对和后续同步到尽调。
```

## 重点文件

请优先修改：

- `src/stores/docRecognition.js`
- `src/pages/DocRecognitionPage.vue`

不要修改：

- 工作台
- 智能尽调
- 智能报告
- 企业探查
- 企业监测
- `token.css`
- 路由

## 当前问题

现在页面虽然左侧默认选中了唐山物桥，但中间文件没有默认选中，导致右侧显示：

```text
选择文件查看识别结果
```

这对 demo 很伤，因为一打开页面最大面积是空态，看起来像没功能。

## P0 必须实现

### 1. 默认选中唐山物桥任务

在 store 初始化时，确保：

```js
currentTaskId = 'dd-ts-wq'
```

不要默认选中杭州智造。

### 2. 默认选中文件

默认选中唐山物桥的：

```text
银行流水摘要.pdf
```

如果文件 id 当前是 `f014`，则：

```js
currentFileId = 'f014'
```

如果实际 id 不同，请按文件名查找，不要硬写错误 id。

### 3. 银行流水摘要不要保持“识别中”

为了 demo 可演示，请把唐山物桥的 `银行流水摘要.pdf` 状态改为：

```text
待确认
```

或者：

```text
已完成
```

推荐用「待确认」，这样可以演示“识别结果需要客户经理确认后同步到尽调”。

### 4. 为银行流水摘要补基础字段识别结果

本轮 P0 不要求非常完整，但至少要让右侧不空，并能看出银行流水价值。

请给 `银行流水摘要.pdf` 增加字段识别 mock：

```text
账户名称：唐山物桥商贸有限公司
开户行：中国民生银行唐山分行
账号：6222 **** **** 8791
流水期间：2025-01 至 2025-12
总入账金额：2,086.32万元
总出账金额：1,934.76万元
月均入账：173.86万元
月均出账：161.23万元
经营净现金流：151.56万元
收入波动率：41.6%
债务本息支出：186.00万元
DSCR：0.81
短期大额进出账：4笔
整数规律交易：12笔
关联账户互转：3笔
临近授信异常转入：2笔
```

置信度大致：

```text
主体/账户/期间/金额：90%+
异常交易/派生指标：80%-88%
```

### 5. 交叉比对 tab 不要空

为唐山物桥任务 `dd-ts-wq` 增加最小可演示的交叉比对数据：

```text
企业主体一致：通过
统一社会信用代码一致：通过
发票收入 vs 纳税申报：差异 4.4%
银行流水 vs 发票收入：差异 8.3%
银行流水 vs 纳税申报收入：差异 4.1%
DSCR：0.81，低于 1，需关注偿债能力
异常流水：短期大额进出账 4 笔、整数规律交易 12 笔、关联账户互转 3 笔
```

AI 初步判断文案：

```text
综合银行流水、发票、纳税申报和工商资料，唐山物桥商贸有限公司主体资料一致，收入数据整体可交叉验证；但 DSCR 为 0.81，现金流覆盖债务本息不足，同时存在短期冲量、整数规律交易和关联互转信号，建议人工复核异常流水和关联账户往来。
```

### 6. 右侧空态优化

即使未来没有选中文件，也不要只有一个巨大空盒子。请将空态文案改成更有引导性的内容：

```text
请选择左侧企业资料包中的文件查看识别结果。
建议优先查看「银行流水摘要.pdf」，可用于收入真实性、经营稳定性和偿债能力核验。
```

但 P0 目标是默认已经选中文件，所以正常进入页面不应看到空态。

## UX 验收

进入页面后，用户看到的首屏应该是：

```text
左侧：唐山物桥商贸有限公司高亮
中间：银行流水摘要.pdf 高亮，状态为待确认/已完成
右侧：字段识别 tab 展示银行流水字段
顶部统计：已完成/待确认/识别中数字合理
```

点击「交叉比对」后：

```text
能看到唐山物桥的交叉比对数据和 AI 判断
不再显示空态
```

## 样式要求

本轮不做大范围 UI 重构，但新增/调整内容必须保持现有项目风格：

- 使用现有 Element Plus 组件。
- 使用当前页面已有 CSS class 或 token.css 变量。
- 不要新增大量硬编码颜色。
- 不要使用 emoji 当主图标。
- 不要引入大面积新视觉样式。
- 不要修改 `token.css`。

## 禁止事项

本轮禁止：

- 不要改其他模块。
- 不要做全页面视觉重构。
- 不要修改路由。
- 不要引入新依赖。
- 不要删除其他企业 demo 数据。
- 不要为了处理终端乱码大面积替换中文。
- 不要破坏上传、确认、同步到尽调按钮。

## 验收步骤

完成后运行：

```bash
npm run build
```

然后自查：

```text
1. 打开资料识别页面
2. 默认选中唐山物桥商贸有限公司
3. 默认选中银行流水摘要.pdf
4. 右侧字段识别不为空
5. 点击交叉比对，能看到唐山物桥交叉比对结果
6. 控制台无报错
7. npm run build 通过
```

## 完成后汇报格式

请按以下格式回复：

```text
资料识别 P0 默认演示态修复完成报告

1. 修改了哪些文件
2. 默认选中的企业和文件是什么
3. 银行流水摘要新增了哪些字段
4. 唐山物桥交叉比对是否可展示
5. 是否未修改其他模块
6. npm run build 是否通过
```

