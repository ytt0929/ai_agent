# OpenClaw 提示词：资料识别 P1.2 交叉比对改为 AI 结论优先

## 背景

资料识别 P1.1 已经完成了交叉比对结果区的基础增强，但当前展示顺序仍然把“交叉比对总览”放在最前面。  
这会让页面更像数据看板，而不是 AI 产品的分析过程。

本次只做一个小收口：让用户进入【交叉比对】tab 后，第一眼先看到 AI 的业务判断，再看收入真实性、经营分析、数据一致性和总览。

## 修改目标

把【资料识别】页面中【交叉比对】tab 的展示顺序调整为：

1. AI分析与复核建议
2. 收入真实性核实
3. 银行流水经营分析
4. 数据一致性验证
5. 交叉比对总览

如果你认为“交叉比对总览”需要保留在首屏，也必须压缩成 AI 分析区内部的轻量摘要，不允许作为首屏第一个独立大区块。

推荐方案：把“交叉比对总览”整体移动到最后。

## 必须保留

不要删除或弱化 P1/P1.1 已经实现的内容：

- AI 分析与复核建议
- 收入真实性核实
- 30% 差异阈值说明
- 银行流水经营分析
- DSCR 指标解释
- 收入波动率说明
- 数据一致性验证
- 字段识别分组
- 确认并同步
- 同步到尽调
- 上传文件
- tab 切换

## 样式要求

必须继续使用项目现有的 Element Plus + token.css 风格：

- 优先使用 `el-card`、`el-table`、`el-tag`、`el-alert`、`el-progress`、`el-descriptions`、`el-tabs`
- 颜色、边框、圆角、间距、字体大小优先引用 `src/styles/token.css` 中已有变量
- 不要新增大面积自定义视觉风格
- 不要使用 emoji 图标
- 不要使用渐变背景
- 不要使用重阴影
- 不要引入孤立的新配色
- 页面要和企业探查、企业监测、智能报告当前 Element Plus 化后的风格保持一致

## 修改范围

优先只修改：

- `src/pages/DocRecognitionPage.vue`

如确实需要调整数据顺序或 computed，可以小范围修改：

- `src/stores/docRecognition.js`

禁止修改：

- 工作台
- 智能尽调
- 智能报告
- 企业探查
- 企业监测
- 路由
- `src/styles/token.css`
- mock 数据的业务含义

## 验收标准

请按以下路径自查：

1. 进入资料识别页面
2. 默认选中唐山物桥商贸有限公司
3. 默认选中银行流水摘要.pdf
4. 点击【交叉比对】tab
5. 首屏第一块必须是【AI分析与复核建议】
6. 页面顺序必须是：
   - AI分析与复核建议
   - 收入真实性核实
   - 银行流水经营分析
   - 数据一致性验证
   - 交叉比对总览
7. 上传、确认并同步、同步到尽调功能不能受影响
8. 页面不能出现 Vue warning / console error
9. 运行 `npm run build` 必须通过

## 完成后请汇报

请明确说明：

1. 修改了哪些文件
2. 交叉比对 tab 顺序是否已变为 AI 分析优先
3. 交叉比对总览放到了哪里
4. 是否保留了 30% 阈值、DSCR、收入波动率说明
5. 是否保留上传、确认并同步、同步到尽调功能
6. 是否未修改其他模块
7. `npm run build` 是否通过
