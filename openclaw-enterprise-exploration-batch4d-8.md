你是资深前端工程师，同时擅长银行业务场景的 UX/UI 设计。请在 `D:\demo\ai-copilot` 项目中继续修正 Phase 2 Batch 4D：企业探查工作区。

## 定位

企业探查是面向银行客户经理的对话式企业数据分析 Agent。

它不是打开后直接生成诊断报告，也不是普通列表查询页。正确体验是：

- 用户先通过自然语言或企业名称/统一社会信用代码发起探查。
- AI 先在对话中识别企业、检查数据覆盖、判断问题类型。
- 简单问题直接在对话中回答。
- 明细、证据链、报告、风险分析等复杂内容在左侧工作区展示。
- 右侧始终是 AI 对话面板，用于追问、解释、生成专项说明、加入报告。
- 报告是显式产物，只有点击“查看报告 / 生成报告”才在左侧打开报告结构。

## 只改这些文件

- `src/pages/EnterpriseExplorationWorkspacePage.vue`
- 如必须补 mock 数据，可少量修改 `src/data/mockEnterpriseSourceData.js`

不要改：

- `src/main.js`
- `src/components/AppSidebar.vue`
- `src/pages/EnterpriseDiagnosisPage.vue`
- `src/pages/EnterpriseDiagnosisListPage.vue`
- 其它业务页面

## 当前主要问题

1. 现在 `EnterpriseExplorationWorkspacePage.vue` 通过 `!hasResult` / `hasResult` 切成两套页面，用户感觉是“突然跳结果页”，不是对话式探查。
2. 聊天消息渲染写了两套：对话优先模式一套，结果模式右侧一套，导致头像、按钮、流程卡、滚动和样式不一致。
3. 点击“查看报告”现在会跳转到 `/enterprise-diagnosis/report/:creditCode`，但企业探查里应该是在左侧工作区打开报告内容，不是跳独立报告页。
4. 点击“查看证据链”也应该优先在左侧工作区打开证据链详情，不要默认跳独立证据链页。
5. 左侧工作区内容太薄，比如“从业/社保概览”只有小卡片，造成大面积空白。
6. 右侧对话按钮和诊断报告页动作不一致，需要统一成：查看报告、查看证据链、生成专项说明、加入报告、授权税票、上传流水。
7. 页面顶部和中间留白过多，结果页布局密度不够，左侧内容没有撑起真实工作区。

## 修改目标

把企业探查工作区调整成一个稳定的两阶段体验：

### 阶段 A：对话发起阶段

未产生结构化结果前，页面保持对话优先。

要求：

- 顶部只保留圆形返回按钮、企业名称/税号/行业标签。
- 中间是对话流，AI 头像保留为蓝色圆形 `AI`。
- 用户输入后，AI 先打字机式输出：
  - 正在识别企业
  - 正在检查数据覆盖
  - 已判断问题类型
- 流程卡只能作为对话中的一条 AI 消息出现，不要独立占据大块页面。
- 流程卡宽度跟 AI 消息区域一致，不要过窄，也不要漂浮在页面中央。

### 阶段 B：结构化结果阶段

当问题需要打开明细、报告、证据链或分析视图时，页面切成左右布局：

- 左侧：结构化工作区
- 右侧：AI 对话面板

要求：

- 不要复制两套聊天渲染。抽出同一套消息结构，至少保证两种模式复用同样的 class、头像、气泡和 actions。
- 右侧对话面板固定在右侧，宽度约 420px。
- 左侧工作区占剩余宽度。
- 左右各自滚动，不要页面整体滚动导致右侧对话被卷走。
- 右侧底部输入框固定在面板底部。

## 左侧工作区需要支持的视图

在 `currentView` 中补齐/整理这些视图：

1. `ops`
   - 标题：经营分析
   - 内容：探查结论、关键数据、异常信号、操作按钮。

2. `taxDeclarations`
   - 标题：申报明细
   - 内容：说明卡 + 申报明细表 + 数据来源 + 缺失/授权提示。

3. `shareholders`
   - 标题：股东明细
   - 内容：股东表格 + 出资比例 + 数据来源。

4. `socialSecurity`
   - 标题：从业/社保概览
   - 内容不能只有一张小卡。至少包括：
     - 结论卡：当前从业人数、是否完整社保明细
     - 指标卡：从业人数、数据来源、是否估算
     - 数据说明：当前来自企业所得税申报表从业人数，不是完整社保缴费明细
     - 操作按钮：查看数据来源、加入报告、上传社保明细

5. `evidenceDetail`
   - 标题：证据链详情
   - 点击“查看证据链”时打开左侧证据链详情。
   - 内容结构：
     - 风险指标标题
     - 数据事实
     - 模型规则
     - 推理结论
     - 指标数据明细
     - 行动建议

6. `taxPanoramaReport`
   - 标题：纳税全景报告
   - 点击“查看报告”或“生成纳税全景报告”时在左侧打开。
   - 内容参考用户给的“企业全景报告”图片，不需要完整还原全部页，但要有真实报告结构：
     - 企业基础信息
     - 纳税概览
     - 申报明细
     - 税负率分析
     - 开票/申报差异
     - 风险提示

7. `diagnosisReport`
   - 标题：企业诊断报告
   - 点击“生成企业诊断报告 / 查看完整诊断报告”时在左侧打开。
   - 可以复用现有 mock 诊断数据，但不要跳转到独立报告页。

## 按钮交互规则

统一右侧 AI 消息 actions：

- “查看报告”
  - 如果当前是纳税/税票问题，设置 `currentView = 'taxPanoramaReport'`
  - 如果当前是风险/诊断问题，设置 `currentView = 'diagnosisReport'`
  - 不要 router.push 到 report 页面。

- “查看证据链”
  - 设置 `currentView = 'evidenceDetail'`
  - 不要 router.push 到 evidence 页面。

- “生成专项说明”
  - 在右侧对话中追加一条 AI 回复。

- “加入报告”
  - 在右侧对话中追加“已加入报告草稿”的提示。

- “授权税票”
  - 只追加 Demo 提示，不做真实接口。

- “上传流水”
  - 只追加 Demo 提示，不做真实上传。

## UI 排版要求

1. 不要让页面出现大块空白。
2. 左侧每个视图至少有 2 到 4 个内容块。
3. 对话阶段最大宽度可以是 760px，但结果阶段必须使用左右布局。
4. 结果阶段左侧不要窄到像卡片列表，右侧不要宽到挤压左侧。
5. AI 流程卡保持轻量，不要放在页面中央作为大状态页。
6. 所有卡片圆角、边框、字号使用现有 token，不要新增一套夸张样式。
7. 不要使用 emoji。

## 编码要求

- 不要大规模重写整个文件。
- 不要用 PowerShell 批量写入大段中文，避免再次产生乱码。
- 优先小范围 patch。
- 保留现有 mock 数据和路由结构。
- 可以新增少量 computed / helper 函数，例如：
  - `openWorkspaceView(view, message)`
  - `openReportView(type)`
  - `openEvidenceView(indicatorId)`
  - `buildAssistantActions(type)`

## 验收标准

完成后运行：

```bash
npm run build
```

页面验收：

1. `/enterprise-diagnosis/workspace/_new?q=近12个月收入趋势如何`
   - AI 先提示输入企业名称或统一社会信用代码。
   - 输入“唐山物桥商贸有限公司”后，不应直接跳空白结果页。
   - 应先在对话中流式显示识别、数据覆盖、问题类型判断。
   - 然后左侧打开“经营分析”，右侧保留对话。

2. 在右侧点击“查看报告”
   - 左侧打开报告结构。
   - 不跳转到 `/enterprise-diagnosis/report/...`。

3. 在右侧点击“查看证据链”
   - 左侧打开证据链详情。
   - 不跳转到 `/enterprise-diagnosis/evidence/...`。

4. 询问“查看社保费明细”
   - 如果只有从业人数数据，左侧显示“从业/社保概览”。
   - 页面不能大面积空白。
   - 需要明确说明当前不是完整社保缴费明细。

5. 右侧 AI 对话：
   - AI 蓝色头像存在。
   - 消息像对话流，不是散落按钮。
   - 输入框固定在右侧面板底部。

6. 构建通过，不能出现 Vue 模板语法错误。
