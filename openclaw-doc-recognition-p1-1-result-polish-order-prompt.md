# OpenClaw 提示词：资料识别 P1.1 结果区顺序与演示态打磨

请读取资料识别模块相关代码，在 P1 已完成的基础上做小范围打磨。本轮不要新增复杂数据，不做全页面重构，只优化右侧结果区的业务呈现顺序、信息密度和演示可读性。

## 重点文件

请优先修改：

- `src/pages/DocRecognitionPage.vue`
- 必要时微调 `src/stores/docRecognition.js`

不要修改其他模块，不要修改 `token.css`，不要改路由。

## 当前状态

P1 已完成：

```text
默认选中唐山物桥 + 银行流水摘要.pdf
字段识别有摘要卡和分组字段
交叉比对有总览卡、数据一致性、收入真实性、银行流水经营分析、AI 判断
```

现在需要优化交叉比对 tab 的展示顺序和演示节奏。

## 核心调整：交叉比对展示顺序

请将交叉比对 tab 的内容顺序调整为：

```text
1. AI 分析与复核建议
2. 收入真实性核实
3. 银行流水经营分析
4. 数据一致性验证
```

不是原来的：

```text
数据一致性验证 → 收入真实性 → 银行流水经营分析 → AI 判断
```

原因：demo 演示应先给业务结论，再展开数据依据。用户第一眼要知道系统判断是什么，而不是先看一堆验证表。

## 页面结构建议

交叉比对 tab 内建议保持：

```text
┌──────────────────────────────┐
│ 交叉比对总览                  │
│ 主体一致 | 收入匹配度 | DSCR | 人工复核 │
└──────────────────────────────┘

┌──────────────────────────────┐
│ AI 分析与复核建议              │
│ 结论摘要：收入整体可交叉验证，但现金流偿债覆盖不足 │
│ 复核建议：异常流水、关联账户、临近授信转入、DSCR │
└──────────────────────────────┘

┌──────────────────────────────┐
│ 收入真实性核实                 │
│ 流水 vs 发票 | 流水 vs 纳税 | 发票 vs 纳税 │
│ 30% 差异阈值说明               │
└──────────────────────────────┘

┌──────────────────────────────┐
│ 银行流水经营分析               │
│ 月均入账 | 月均出账 | 波动率 | DSCR | 异常交易 │
└──────────────────────────────┘

┌──────────────────────────────┐
│ 数据一致性验证                 │
│ 企业主体 / 信用代码 / 文件来源 / 详细验证表 │
└──────────────────────────────┘
```

## AI 分析区要求

AI 分析与复核建议区需要更像“业务结论卡”，不要只是普通列表。

建议包含：

```text
结论摘要：
综合银行流水、发票、纳税申报和工商资料，唐山物桥商贸有限公司主体资料一致，收入数据整体可交叉验证；但 DSCR 为 0.81，现金流覆盖债务本息不足，同时存在短期冲量、整数规律交易和关联互转信号。

关键判断：
- 收入真实性：未超过 30% 差异阈值，整体可验证
- 经营稳定性：收入波动率 41.6%，波动偏高
- 偿债能力：DSCR 0.81，低于 1
- 人工复核：短期大额进出账、整数规律交易、关联账户互转、临近授信异常转入
```

UI 建议：

- 用 `el-alert` 展示结论摘要。
- 用小型 `el-tag` 标出“收入真实性”“经营稳定性”“偿债能力”“人工复核”。
- 不要使用大面积红色警告，避免页面显得过度危险。

## 演示态可读性要求

请检查右侧结果区是否过于拥挤，并做轻量优化：

1. 交叉比对总览卡保持紧凑，不要占满过高空间。
2. 每个业务区块标题清晰，标题下不要堆太多说明文字。
3. 指标卡数量多时允许换行，但卡片高度要稳定。
4. 数据一致性验证放到最后后，可以保持表格展示，不必过度展开。
5. 右侧内容区滚动应稳定，不要把整个页面撑坏。

## 字段识别 tab 不要大改

字段识别 tab 本轮只允许轻微优化：

- 如果摘要卡太高，可以压缩高度。
- 如果同步提示位置太靠下，可以放到摘要卡下方。
- 不要改字段分组逻辑。
- 不要删掉 DSCR 和收入波动率解释。

## 样式要求

必须保持 Element Plus + token.css 风格：

- 使用 `el-card`、`el-alert`、`el-tag`、`el-table`、`el-progress`
- 使用项目 token 变量
- 不要新增大面积硬编码颜色
- 不要大量 emoji
- 不要强阴影、渐变、大圆角
- 和企业探查、企业监测、智能报告页面密度一致

## 禁止事项

本轮禁止：

- 不要新增复杂 mock 数据。
- 不要改工作台、智能尽调、智能报告、企业探查、企业监测。
- 不要改路由。
- 不要改 `token.css`。
- 不要引入新依赖。
- 不要做全页面大重构。
- 不要为了处理终端乱码大面积替换中文。
- 不要破坏上传、确认、同步到尽调功能。

## 验收步骤

完成后运行：

```bash
npm run build
```

并自查：

```text
1. 打开资料识别页面
2. 默认选中唐山物桥 + 银行流水摘要.pdf
3. 点击交叉比对
4. 顺序应为：AI分析 → 收入真实性核实 → 银行流水经营分析 → 数据一致性验证
5. AI分析区第一眼能讲清：收入可验证、DSCR不足、异常流水需复核
6. 30% 差异阈值仍保留
7. DSCR 和收入波动率仍保留
8. 页面无明显拥挤、滚动异常
9. npm run build 通过
```

## 完成后汇报格式

请按以下格式回复：

```text
资料识别 P1.1 结果区顺序与演示态打磨完成报告

1. 修改了哪些文件
2. 交叉比对 tab 的展示顺序是否已调整
3. AI 分析区如何展示业务结论
4. 是否保留收入真实性、银行流水经营分析、数据一致性验证
5. 是否保留 30% 阈值、DSCR、收入波动率解释
6. 是否未修改其他模块
7. npm run build 是否通过
```

