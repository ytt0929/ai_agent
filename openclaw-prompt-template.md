# OpenClaw Prompt Template

```text
你是资深前端工程师和 UI/UX 工程师。

项目路径：
D:\demo\ai-copilot

请只读取本提示词，不要读取旧提示词文件。

阶段定位：
说明当前任务属于哪个阶段、哪个 Batch。
说明当前页面/模块的产品定位。
说明本轮只解决什么问题，不解决什么问题。

示例：
当前任务属于第二阶段 Batch 4A：企业诊断报告页布局收口。
这是企业诊断报告页，不是文档编辑页，也不是证据链详情页。
本轮目标是把页面改成“左侧诊断报告内容 + 右侧 AI 风险研究助手”的分析工作台。

重要限制：
- 只修改：写清楚允许修改的文件。
- 不要修改：store、mock 数据、路由、全局样式、侧边栏、其他页面。
- 不要引入新依赖。
- 不要新建不在本轮范围内的页面。
- 不要处理编码问题，浏览器中文正常。
- 保留现有业务能力和数据结构。

当前问题：
1. 用业务语言说明当前页面哪里不符合预期。
2. 用 UI/UX 语言说明布局、密度、层级、交互或内容的问题。
3. 明确哪些问题是本轮必须解决的。
4. 明确哪些问题本轮不解决。

目标效果：
1. 用户打开页面后应该先看到什么。
2. 页面主体应该采用什么布局。
3. 用户能完成哪些核心任务。
4. 改完后应该达到什么业务感受或视觉效果。

具体修改：

1. 修改区域一
- 指定相关 class、组件区域或页面结构。
- 说明要怎么改布局、内容、样式或交互。
- 说明不能破坏哪些现有能力。

2. 修改区域二
- 指定相关 class、组件区域或页面结构。
- 说明数据如何展示。
- 说明点击、筛选、抽屉、对话等联动要求。

3. 修改区域三
- 继续列出本轮必要修改。
- 避免写可选方案，减少 OpenClaw 推理成本。

不要改：
- 明确列出禁止修改的文件或模块。
- 明确列出禁止新增的能力。
- 明确列出不要重构、不迁移、不改数据结构。

验收标准：
1. npm run build 通过。
2. 只修改指定文件。
3. 页面布局达到目标效果。
4. 关键交互可用。
5. 不破坏原有业务能力。
6. 没有新增不在范围内的页面、路由、依赖或全局样式。

完成后回复：
- 修改了哪个文件。
- 完成了哪些点。
- npm run build 结果。
- 如果有未完成项，明确说明原因。
```

## 使用原则

- 每次只写一个 Batch 或一个明确补丁任务。
- 优先指定文件、class、区域和交互，不写过多背景。
- 保留“阶段定位、重要限制、当前问题、目标效果、具体修改、验收标准”六段。
- 如果 OpenClaw 执行慢，先缩短“当前问题”和“目标效果”，不要删除“重要限制”和“验收标准”。
- 不要让同一版提示词同时要求改多个不相关页面。
