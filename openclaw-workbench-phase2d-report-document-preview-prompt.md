# OpenClaw 提示词：Phase 2-D 尽职调查报告文档化预览

请读取并严格执行本文件：

`openclaw-workbench-phase2d-report-document-preview-prompt.md`

## 背景

Phase 2-C 已完成：

- 产物确认页中产物名称统一为「尽职调查报告」
- `查看` 已改为左侧工作区内嵌预览面板，不覆盖右侧 AI Copilot
- 报告目录使用智能报告模板 `credit-v2021` 的 15 章目录
- `证据链文件` 支持「查看 / 生成」
- 构建通过

现在进入 Phase 2-D，只做「产物确认页查看报告」的文档化预览增强。

核心目标：

> 点击产物确认面板中「尽职调查报告」的【查看】按钮后，左侧工作区出现一份类似 PDF/Word 文档的可视化报告预览，而不是普通系统卡片。  
> 这份报告叫「尽职调查报告」，内容使用唐山物桥商贸有限公司的 demo 数据，章节目录仍然和智能报告模板保持一致。

## 只允许重点修改的文件

优先只修改：

1. `src/components/workbench/artifacts/DeliverablesArtifact.vue`

如确实需要补充报告 mock 数据，可少量修改：

2. `src/stores/workbenchAssistant.js`

不要修改：

- 工作台主布局 `WorkbenchPage.vue`
- 12 阶段状态机主流程
- 独立业务模块页面
- 路由
- `mockSmartReport.js` 的模板结构

## 本轮要实现什么

### 1. 「尽职调查报告 / 查看」显示文档化报告预览

当前点击「尽职调查报告 / 查看」后，左侧工作区已有内嵌预览面板。

请将报告预览区域升级为「文档/PDF 样式」：

```text
┌──────────────────────────────────────────────────────────┐
│ 报告预览：尽职调查报告                              [关闭] │
├──────────────────────────────────────────────────────────┤
│                                                          │
│                 尽职调查报告                             │
│                                                          │
│        企业名称：唐山物桥商贸有限公司                    │
│        报告模板：尽职调查报告                            │
│        综合评分：72    等级：C+    风险等级：中风险        │
│        资料完整度：86%    生成日期：2026-07-02             │
│                                                          │
│  一、履职声明与基本信息                                  │
│  本报告基于客户访谈、企业资料、工商司法查询、税票采集、   │
│  资料识别及风险诊断结果生成，用于授信审批参考。           │
│                                                          │
│  二、重要说明事项                                        │
│  当前报告仍有若干待确认事项，包括税负异常说明、购销两头   │
│  在外业务解释、授信额度条件确认等。                       │
│                                                          │
│  三、行内评级及授信情况                                  │
│  综合评分 72 分，等级 C+，风险等级中风险。                │
│                                                          │
│  ……                                                      │
│                                                          │
│                         第 1 页 / 共 3 页                 │
└──────────────────────────────────────────────────────────┘
```

视觉要求：

- 像一份白色 A4 文档放在浅色工作区里
- 文档区域使用白底、细边框、轻阴影或无阴影
- 文档宽度控制在 760px - 860px 左右，居中展示
- 有报告标题、企业信息、报告摘要、章节正文
- 可滚动浏览整份报告
- 不要使用花哨大图标
- 不要做成营销页
- 不要覆盖右侧 AI Copilot

### 2. 保留左侧目录，但目录要像文档目录

当前预览里已有 15 章目录。请保留目录，但调整为更像文档导航：

```text
报告目录
一  履职声明与基本信息
二  重要说明事项
三  行内评级及授信情况
四  申请人基本信息
...
十五 附件清单
```

要求：

- 目录在文档预览左侧或上方均可，推荐左侧窄栏 + 右侧文档页
- 点击目录项，右侧文档滚动或切换到对应章节
- 目录状态标签可以保留，但不要太抢眼
- 目录文字、间距、色彩要和项目风格一致

### 3. 生成一版完整「尽职调查报告」demo 文档

在报告预览正文里，不要只显示当前单章摘要，要给用户看到“这真的是一份报告”。

请基于现有 `reportSections` 生成一份可视化文档，至少展示以下部分：

#### 封面信息

```text
尽职调查报告

企业名称：唐山物桥商贸有限公司
所属行业：建材批发 / 商贸流通
所在区域：河北省唐山市
报告模板：尽职调查报告
综合评分：72
等级：C+
风险等级：中风险
资料完整度：86%
报告状态：底稿已生成，待确认和编辑
生成日期：2026-07-02
```

#### 摘要

```text
本报告基于工商登记信息、司法公开数据、税票采集数据、企业补充资料及 AI 风险诊断结果生成。
经初步核验，唐山物桥商贸有限公司主体状态正常存续，纳税信用评级 A 级，
但存在税负率显著低于行业、开票收入与申报收入不一致、购销两头在外、短期偿债压力较大等风险事项。
建议作为有条件授信对象，后续需补充税负异常说明、确认购销业务真实性，并强化贷后监测。
```

#### 章节正文

章节必须沿用 15 章模板：

1. 履职声明与基本信息
2. 重要说明事项
3. 行内评级及授信情况
4. 申请人基本信息
5. 股权结构及实控人
6. 经营情况
7. 财务状况
8. 收入真实性核实
9. 信用状况
10. 行业地位比较
11. 诉讼与负面信息
12. 主要风险分析
13. 授信额度依据
14. 调查结论与授信方案
15. 附件清单

每章至少展示：

- 章节标题
- 1-3 段正文
- 资料依据标签
- 本章证据链标签
- 如有待确认项，展示待确认项

可以直接复用 `props.data.reportSections` 的 `content`、`materials`、`evidence`、`pending`。

### 4. 报告语言要更像正式文档

当前很多内容偏“系统说明”。请在报告预览里把语言处理成正式报告语气：

不要这样：

```text
暂无内容
当前章节内容待生成
demo 阶段模拟
```

应该这样：

```text
根据已获取资料，本章对申请人基本情况进行核验。申请人唐山物桥商贸有限公司主体状态为正常存续，注册资本 500 万元，主营建材批发及商贸流通业务。
```

注意：

- 右侧 AI 对话可以继续是口语化
- 左侧报告预览必须是正式文档语气
- 不要出现“mock”“demo”“模拟”等字样在报告正文中

### 5. 预览和编辑的关系

保持当前逻辑：

- 「查看」= 文档化只读预览
- 「编辑」= 进入 `ReportEditorArtifact` Lite
- 「导出报告」= 基于当前尽职调查报告底稿导出

文档预览底部保留按钮：

```text
[进入编辑] [导出报告] [关闭预览]
```

点击「进入编辑」继续触发 `emit('edit-report')`。

### 6. 非报告产物预览保持轻量

本轮不要大改非报告产物。

以下产物仍然可以使用轻量结构化预览：

- 工商核验报告
- 司法查询报告
- 税票分析报告
- 风险诊断报告
- 证据链文件

只要标题正确、不跳转、不覆盖右侧 AI 即可。

### 7. 样式要求

必须使用项目现有 UI 风格和 Element Plus 组件。

要求：

- 用 `el-card`、`el-tag`、`el-button`、`el-descriptions`、`el-divider` 等 Element Plus 组件
- 使用项目 token：`var(--text-primary)`、`var(--text-secondary)`、`var(--border-divider)`、`var(--bg-card)`、`var(--bg-page)`、`var(--color-primary)` 等
- 文档页可以使用少量 fallback 色值，例如 `var(--bg-card, #fff)`
- 不要新增大量 icon
- 不要大面积使用渐变、彩色块、花哨阴影
- 文档正文排版要清晰：标题、段落、表格、标签间距自然
- 字号控制在企业后台风格，不要过大

## 建议实现方式

在 `DeliverablesArtifact.vue` 中新增一组内部组件结构或函数：

```vue
<div v-if="previewItem?.type === 'report'" class="report-document-preview">
  <aside class="report-document-toc">...</aside>
  <section class="report-document-page">
    <div class="report-cover">...</div>
    <section
      v-for="section in previewSections"
      :key="section.id"
      :id="`report-section-${section.id}`"
      class="report-document-section"
    >
      ...
    </section>
  </section>
</div>
```

目录点击可以简单实现：

```js
function scrollToReportSection(id) {
  activePreviewChapter.value = id
  requestAnimationFrame(() => {
    document.getElementById(`report-section-${id}`)?.scrollIntoView({
      behavior: 'smooth',
      block: 'start'
    })
  })
}
```

如果担心 `id` 全局冲突，可以加前缀。

## 验收标准

修改后请验证：

1. `npm run build` 必须通过
2. 点击产物确认页「尽职调查报告 / 查看」：
   - 左侧工作区出现白色文档页式预览
   - 不覆盖右侧 AI Copilot
   - 标题为「报告预览：尽职调查报告」
   - 有封面信息
   - 有摘要
   - 有 15 章正文
   - 正文语言像正式报告，不出现 mock/demo/模拟
3. 点击目录项能定位或切换章节
4. 文档底部有「进入编辑」「导出报告」「关闭预览」
5. 点击「进入编辑」仍能进入 `ReportEditorArtifact`
6. 非报告产物「查看」仍可打开轻量预览
7. 没有修改独立业务模块页面

## 输出报告格式

完成后请输出：

```text
Phase 2-D Report Document Preview 完成报告

1. 修改了哪些文件
2. 尽职调查报告查看是否已经变成文档/PDF 样式预览
3. 文档预览包含哪些内容
4. 15 章目录是否保留并可定位
5. 是否影响右侧 AI Copilot
6. 非报告产物预览是否仍正常
7. npm run build 是否通过
8. 是否修改了独立业务模块
```

